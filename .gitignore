forAiTools.md
*.gguf
tavily-quickstart

# Python
.venv
__pycache__/
*.pyc
*.pyo
*.egg-info/

# 环境变量（密钥，永不入库）
.env

# LLM 提供方配置（含用户账号信息，模板见 config/providers.example.toml）
config/providers.toml

# 退休模型记录（机器维护，`aris llm fetch`/`retired` 运行时读写）
config/retired_models.toml

# 运行时数据（不进 git，备份见 README「数据与备份」）
data/

# 测试与静态工具缓存（pytest/ruff 等启用后自动产生）
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# 编辑器 / 系统杂项
.vscode/
.idea/
*.swp
.DS_Store

# 旧版备份（迁移留档，不入库）
*.bak

# 构建产物
build/
dist/

# C 编译产物
*.so
*.o
*.dll
