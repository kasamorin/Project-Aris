"""Aris —— 拟人 AI，目标是社会学意义上的人。"""

# 版本号唯一源（AGENTS.md「版本号更新」）：
#   pyproject.toml 用 dynamic version 从本行读取，uv.lock 不记录本项目版本。
#   改版本请用 `bash scripts/bump-version.sh <版本|patch|minor|major>`，不要手改别处。
__version__ = "0.4.2"
